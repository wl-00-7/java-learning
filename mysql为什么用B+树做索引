1：使用索引时为了减少I/O次数，所以要求树矮
2：数组查询快但是插入删除O(n)，不适合数据库的使用，排除
3：二叉树会退化到链表以及和平衡树一样，太高，I/O次数太多
4：B树矮，但是数据封存在所有的节点，范围查询时要遍历整棵树，I/O次数不稳定
5：综合来看B+树胜出，数据只存在叶子节点，内部节点只存索引->节点可以存更多的key->树更矮；
叶子节点用双向链表链接->范围查询时只要顺着链表扫描，I/O次数稳定且少
